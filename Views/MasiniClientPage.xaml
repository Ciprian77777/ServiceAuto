<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ServiceAuto.Views.MasiniClientPage"
             Title="Mașinile clientului">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="➕ Adaugă" 
                     Clicked="OnAdaugaMasinaClicked" />
    </ContentPage.ToolbarItems>

    <CollectionView x:Name="MasiniCollectionView"
                    SelectionMode="None">

        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame Padding="15"
                       Margin="10,5"
                       CornerRadius="10"
                       BackgroundColor="White"
                       HasShadow="True">

                    <VerticalStackLayout Spacing="8">

                        <Label Text="{Binding DisplayText}"
                               FontSize="16"
                               FontAttributes="Bold"
                               TextColor="#2C3E50" />

                        <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto">

                            <Label Grid.Row="0" Grid.Column="0"
                                   Text="{Binding NrInmatriculare}"
                                   FontSize="14"
                                   FontAttributes="Bold"
                                   TextColor="#3498DB" />

                            <Label Grid.Row="0" Grid.Column="1"
                                   Text="{Binding AnFabricatie}"
                                   FontSize="14"
                                   TextColor="#7F8C8D"
                                   HorizontalOptions="End" />

                            <Label Grid.Row="1" Grid.Column="0"
                                   Text="{Binding Culoare}"
                                   FontSize="12"
                                   TextColor="#7F8C8D" />

                            <Label Grid.Row="1" Grid.Column="1"
                                   Text="{Binding Kilometraj, StringFormat='{0:N0} km'}"
                                   FontSize="12"
                                   FontAttributes="Bold"
                                   TextColor="#E74C3C"
                                   HorizontalOptions="End" />

                        </Grid>

                    </VerticalStackLayout>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>

    </CollectionView>
</ContentPage>