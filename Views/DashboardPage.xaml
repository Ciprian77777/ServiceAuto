<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ServiceAuto.Views.DashboardPage"
             Title="Dashboard">

    <VerticalStackLayout Spacing="20" Padding="20">

        <Label x:Name="WelcomeLabel"
               Text="Bun venit!"
               FontSize="24"
               FontAttributes="Bold"
               HorizontalOptions="Center" />

        <Label x:Name="DateLabel"
               Text=""
               FontSize="14"
               TextColor="Gray"
               HorizontalOptions="Center" />

        <Grid ColumnDefinitions="*,*" ColumnSpacing="15">

            <Frame BackgroundColor="#3498DB"
                   Padding="20"
                   CornerRadius="10">
                <VerticalStackLayout Spacing="5">
                    <Label x:Name="PieseCountLabel"
                           Text="0"
                           FontSize="28"
                           TextColor="White"
                           FontAttributes="Bold"
                           HorizontalOptions="Center" />
                    <Label Text="Piese"
                           FontSize="14"
                           TextColor="White"
                           HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Frame>

            <Frame BackgroundColor="#2ECC71"
                   Padding="20"
                   CornerRadius="10">
                <VerticalStackLayout Spacing="5">
                    <Label x:Name="ClientiCountLabel"
                           Text="0"
                           FontSize="28"
                           TextColor="White"
                           FontAttributes="Bold"
                           HorizontalOptions="Center" />
                    <Label Text="Clienți"
                           FontSize="14"
                           TextColor="White"
                           HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Frame>

        </Grid>

        <Button Text="📦 Piese de schimb"
                Clicked="OnPieseClicked"
                BackgroundColor="#3498DB"
                TextColor="White"
                HeightRequest="50"
                CornerRadius="10" />

        <Button Text="DECONECTARE"
                Clicked="OnLogoutClicked"
                BackgroundColor="#E74C3C"
                TextColor="White"
                HeightRequest="50"
                CornerRadius="10" />

        <Button Text="👥 Clienți"
        Clicked="OnClientClicked"
        BackgroundColor="#9B59B6"
        TextColor="White"
        HeightRequest="50"
        CornerRadius="10"
        Margin="0,10,0,0" />

    </VerticalStackLayout>
</ContentPage>