<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ServiceAuto.Views.DetaliiClientPage"
             Title="Detalii Client">

    <ScrollView>
        <StackLayout Padding="20" Spacing="15">

            <Frame CornerRadius="10" Padding="15">
                <StackLayout Spacing="10">
                    <Label Text="👤 Detalii Client" 
                           FontSize="20" 
                           FontAttributes="Bold"/>

                    <StackLayout Spacing="5">
                        <Label Text="{Binding Client.Nume}" 
                               FontSize="16"
                               FontAttributes="Bold"/>
                        <Label Text="{Binding Client.Email}" 
                               FontSize="14"/>
                        <Label Text="{Binding Client.Telefon}" 
                               FontSize="14"/>
                        <Label Text="{Binding Client.Adresa}" 
                               FontSize="14"/>
                    </StackLayout>
                </StackLayout>
            </Frame>

            <Frame CornerRadius="10" Padding="15">
                <StackLayout Spacing="10">
                    <Label Text="📊 Statistici" 
                           FontSize="18" 
                           FontAttributes="Bold"/>

                    <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" 
                          RowSpacing="10" ColumnSpacing="10">

                        <Frame Grid.Row="0" Grid.Column="0" 
                               CornerRadius="10" Padding="10">
                            <StackLayout Spacing="5">
                                <Label Text="🚗 Mașini" 
                                       FontSize="14" 
                                       TextColor="Gray"/>
                                <Label x:Name="MasiniCountLabel" 
                                       Text="0" 
                                       FontSize="24" 
                                       FontAttributes="Bold"
                                       HorizontalOptions="Center"/>
                            </StackLayout>
                        </Frame>

                        <Frame Grid.Row="0" Grid.Column="1" 
                               CornerRadius="10" Padding="10">
                            <StackLayout Spacing="5">
                                <Label Text="🔧 Intervenții" 
                                       FontSize="14" 
                                       TextColor="Gray"/>
                                <Label x:Name="InterventiiCountLabel" 
                                       Text="0" 
                                       FontSize="24" 
                                       FontAttributes="Bold"
                                       HorizontalOptions="Center"/>
                            </StackLayout>
                        </Frame>

                    </Grid>
                </StackLayout>
            </Frame>

            <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                <Button Grid.Column="0"
                        Text="🚗 Vezi mașini"
                        Clicked="OnMasiniClicked"
                        BackgroundColor="#2196F3"
                        TextColor="White"
                        CornerRadius="10"/>

                <Button Grid.Column="1"
                        Text="🔧 Vezi intervenții"
                        Clicked="OnInterventiiClicked"
                        BackgroundColor="#4CAF50"
                        TextColor="White"
                        CornerRadius="10"/>
            </Grid>

        </StackLayout>
    </ScrollView>

</ContentPage>