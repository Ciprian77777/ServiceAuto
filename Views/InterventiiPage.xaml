<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ServiceAuto.Views.InterventiiPage"
             Title="Intervenții">

    <VerticalStackLayout Spacing="20" Padding="20">

        <Label Text="🔧 Intervenții"
               FontSize="24"
               FontAttributes="Bold"
               HorizontalOptions="Center" />

        <Button Text="➕ Adaugă intervenție nouă"
                Clicked="OnAdaugaInterventieClicked"
                BackgroundColor="#3498DB"
                TextColor="White"
                HeightRequest="50"
                CornerRadius="10" />

        <Button Text="📅 Vezi intervențiile de azi"
                Clicked="OnInterventiiAziClicked"
                BackgroundColor="#2ECC71"
                TextColor="White"
                HeightRequest="50"
                CornerRadius="10" />

        <Label x:Name="StatusLabel"
               Text=""
               HorizontalOptions="Center"
               Margin="20" />

    </VerticalStackLayout>
</ContentPage>