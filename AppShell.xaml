<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:ServiceAuto.Views"
       x:Class="ServiceAuto.AppShell"
       Title="ServiceAuto"
       FlyoutBehavior="Disabled">

    <Shell.FlyoutHeader>
        <StackLayout BackgroundColor="#2196F3" HeightRequest="100">
            <Label Text="Service Auto" 
                   TextColor="White" 
                   FontSize="24"
                   HorizontalOptions="Center"
                   VerticalOptions="Center"/>
        </StackLayout>
    </Shell.FlyoutHeader>

    <ShellContent Route="LoginPage"
                  ContentTemplate="{DataTemplate views:LoginPage}" />

    <FlyoutItem Title="🏠 Dashboard">
        <ShellContent Route="DashboardPage"
                      ContentTemplate="{DataTemplate views:DashboardPage}" />
    </FlyoutItem>

    <FlyoutItem Title="🔧 Intervenții">
        <ShellContent Route="InterventiiPage"
                      ContentTemplate="{DataTemplate views:InterventiiPage}" />
    </FlyoutItem>

    <FlyoutItem Title="👥 Clienți">
        <ShellContent Route="ClientiPage"
                      ContentTemplate="{DataTemplate views:ClientiPage}" />
    </FlyoutItem>

    <FlyoutItem Title="🚗 Mașini">
        <ShellContent Route="MasiniPage"
                      ContentTemplate="{DataTemplate views:MasiniPage}" />
    </FlyoutItem>

    <FlyoutItem Title="⚙️ Piese">
        <ShellContent Route="PiesePage"
                      ContentTemplate="{DataTemplate views:PiesePage}" />
    </FlyoutItem>

    <FlyoutItem Title="👤 Contul meu">
        <ShellContent>
            <views:ContPage />
        </ShellContent>
    </FlyoutItem> 

    <ShellContent Route="InregistrarePage"
                  ContentTemplate="{DataTemplate views:InregistrarePage}" />

    <ShellContent Route="AdaugaInterventiePage"
                  ContentTemplate="{DataTemplate views:AdaugaInterventiePage}" />

    <ShellContent Route="AdaugaClientPage"
                  ContentTemplate="{DataTemplate views:AdaugaClientPage}" />

    <ShellContent Route="DetaliiClientPage"
                  ContentTemplate="{DataTemplate views:DetaliiClientPage}" />

    <ShellContent Route="InterventiiClientPage"
                  ContentTemplate="{DataTemplate views:InterventiiClientPage}" />

</Shell>